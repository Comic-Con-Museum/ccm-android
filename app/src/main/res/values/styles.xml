<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/light_blue</item>
        <item name="colorPrimaryDark">@color/dark_blue</item>
        <item name="colorAccent">@color/light_yellow</item>
        <item name="actionBarTheme">@style/ActionBarTheme</item>

        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="ActionBarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="colorControlNormal">@color/darkText</item>
        <item name="android:textColor">@color/darkText</item>
    </style>

    <style name="ProgressIncomplete">
        <item name="android:textColor">@color/light_yellow</item>
        <item name="android:background">@drawable/progress_background_incomplete</item>
    </style>

    <style name="ProgressComplete">
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@drawable/progress_background_complete</item>
    </style>


</resources>
